{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "build": "pulp build -- --censor-lib --strict",
    "test": "pulp test",
    "revoked": "pulp build --main Revoked --to ../wwwroot/js/app.js",
    "doc": "pulp docs -- --format html"
  },
  "devDependencies": {
    "bower": "^1.8.8",
    "npm-run-all": "^4.1.3",
    "pulp": "^12.2.0",
    "purescript": "^0.12.0",
    "purescript-psa": "^0.6.0",
    "rimraf": "^2.6.2"
  },
  "dependencies": {
    "xmlhttprequest": "^1.8.0"
  }
}
