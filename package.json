{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "clean:output": "rimraf output",
    "clean:dist": "rimraf dist",
    "clean:doc": "rimraf generated-docs",
    "clean": "npm-run-all clean:*",
    "build": "pulp build -- --censor-lib --strict",
    "test": "pulp test",
    "projectArken:endpoint": "cpx 'endpoint/*' dist",
    "projectArken:build": "pulp build --main ProjectArken --to dist/app.js",
    "projectArken": "npm-run-all projectArken:*",
    "doc": "pulp docs -- --format html"
  },
  "devDependencies": {
    "bower": "^1.8.8",
    "cpx": "^1.5.0",
    "npm-run-all": "^4.1.3",
    "pulp": "^12.2.0",
    "purescript": "^0.12.0",
    "purescript-psa": "^0.6.0",
    "rimraf": "^2.6.2"
  },
  "dependencies": {}
}
